
const INITIAL_STATE = {
    folders: []
};

const folderReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default folderReducer;